#spring.datasource.url=jdbc:postgresql://${DB_HOST}:5432/${DB_NAME}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}

spring.datasource.url=jdbc:postgresql://localhost:5432/xpress
spring.datasource.username=postgres
spring.datasource.password=saheedajayi
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#access_expiration=${access_token}
#refresh_expiration=${refresh_token}

#Jwt_Secret_Key=${SECRET_KEY}

access_expiration=86400
refresh_expiration=7776000
Jwt_Secret_Key=secret
